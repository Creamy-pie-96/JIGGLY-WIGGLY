#pragma once
#include "Player.hpp"

class physics_engine
{
private:
    /* data */
public:
    physics_engine(/* args */);
    ~physics_engine();
};

physics_engine::physics_engine(/* args */)
{
}

physics_engine::~physics_engine()
{
}
